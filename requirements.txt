# ─── Runtime (needed to run the Streamlit app) ──────────────────
streamlit>=1.35
duckdb>=1.0
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
python-dotenv>=1.0

# LLM providers — at least one is required for the Chat feature
openai>=1.30
anthropic>=0.40
google-genai>=1.0

# ─── Build-time only (needed for run_refresh.py / data pipeline) ─
openpyxl>=3.1
pypdf>=6.0
PyYAML>=6.0
